apiVersion: v1
kind: Service
metadata:
  name: go-postgresql-app
  namespace: go-postgresql-dev
  labels:
    app: go-postgresql-app
spec:
  selector:
    app: go-postgresql-app
  ports:
  - port: 80
    targetPort: 80
    name: http
    protocol: TCP
  type: LoadBalancer
  sessionAffinity: None
