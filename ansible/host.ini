# Ansible Inventory File for go-postgresql-dev
# This file defines the Kubernetes control plane (localhost with kubectl access)

[local]
localhost ansible_connection=local

[k8s:children]
local

[k8s:vars]
ansible_python_interpreter=/usr/bin/python3
kubeconfig_path=~/.kube/config
